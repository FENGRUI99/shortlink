spring:
  application:
    name: short-link-admin
#  profiles:
#    active: prd

short-link:
  group:
    max-num: 20
  flow-limit:
    enable: true
    time-window: 1
    max-access-count: 20

logging:
  pattern:
    level: debug
  config: classpath:logback-spring.xml
